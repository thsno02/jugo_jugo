# 旧产物归档

这个目录保存已经降级为旧产物的两轮 KB 产物。它们仍然有审计价值，但不再代表当前要建设的 KB 形态。

## v0_meta_kb_initialization_demo_20260524

路径：`legacy/v0_meta_kb_initialization_demo_20260524/`

问题：

- 把“如何生产 KB 的机制”误当成了 LLM Wiki 主题本身。
- 内容是“生产机制”的元 KB，不是目标主题 KB。
- 可以证明版本束、引用、出处论证、影响队列等工程机制能跑通，但不能证明知识生产方向正确。

保留价值：

- 第一个机制验证 demo。
- 可用于审计文件契约、版本束、引用图、动态检索记录和中文化处理。

## v1_topic_hub_skeleton_20260524

路径：`legacy/v1_topic_hub_skeleton_20260524/`

问题：

- 核心生产对象错了，把原子事实知识卡跑成了主题/枢纽节点。
- 采用自上而下的主题覆盖，而不是自下而上的事实生产。
- 知识卡写作像中间流程产物，不像可读的 zet 风格原子事实知识卡。
- 出处论证、审计、发布流程的痕迹侵入了知识卡，导致人类审计和阅读体验变差。
- 强校验放在枢纽/主题节点发布上，而不是放在原子事实成立上。
- 循环周期过重，每个主题都走了来源挖掘、规划、生成、审计、采纳、视图、技能评估的完整发布流程。

保留价值：

- 可作为流程 demo 和注意力地图。
- 可用于审计 agent 编排、执行者边界、脚注布局、选中版本元数据、审计越界等过程问题。
- 不作为当前 KB 主体，不继续在该结构上补枢纽页。

## 当前方向

当前方向切换为 `llm_wiki/` 下的原子事实 KB。

原则：

- 当前循环的重点是生长可读、可追溯、经出处论证做实的原子事实知识卡。
- 不做枢纽页，不做聚类，不做主题覆盖。
- graph/link 是关系建模，不是知识本身。
- 出处论证是事实生产的组成部分，用来说明知识卡为什么可以暂时被接受为事实。

## 语言边界

当前新写的说明、审计报告和活跃工作文档以中文为主语言。`legacy/v0_*` 与 `legacy/v1_*` 里面的原始运行产物作为证据快照保留原貌，其中可能仍有英文标题、状态码和过程记录；除非进入新的重写流程，否则不直接翻译这些历史快照，以免改变审计对象。
