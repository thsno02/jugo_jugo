# 出处论证：LLM Wiki 作为模式文件

## 结论

这张草稿卡可以暂时被当作 `known_fact`，因为任务指定来源的开头直接说明了 “LLM Wiki” 的标题、模式定位和该文件的用途。卡片只整理这组自我描述，不扩展到其它实现、背景或评价。

## 事实从哪里来

事实来自 `data/raw/gist_raw/karpathy-gist-llm-wiki/raw.txt:1-5`。这些行包含标题、对 “LLM Wiki” 的一句模式描述，以及对该文件用途的说明。

候选字段通过 `llm_wiki/loop/iterations/iteration_20260525_0002_source_mining_karpathy_gist/artifacts/fact_candidates.md` 中的 `候选 1` 核对；该文件未用于引入额外事实。

## 来源为什么能支撑它

来源第 1 行给出标题 “LLM Wiki”。第 3 行把它描述为用 LLM 构建个人知识库的模式。第 5 行说明该文件是 idea file，面向用户自己的 LLM Agent，目标是传达高层想法，而具体部分由 agent 与用户协作展开。

因此，卡片中的 “模式” 和 “文件用途” 都来自来源开头的明示内容。卡片把来源中的英语说明整理成中文原子事实，但没有加入其它背景判断。

## 成立范围

该事实只在一个范围内成立：这个来源如何描述自己的文档目的，以及如何在开头定位 “LLM Wiki” 这个概念。它不证明 “LLM Wiki” 是通用术语，也不说明任何具体实现已经完成。

## 为什么仍是 draft

当前只使用任务指定的一处来源证据，且本轮职责只是起草知识卡，不负责采纳、合并或做跨来源核验。因此状态保持为 `draft`。
