# Iteration 20260525 0000 Bootstrap

## Focus

Create the v3 capsule and register it as the active loop.

## Frozen Variables

- No card production yet.
- No v2 candidate import yet.
- Root `llm_wiki/` remains absent.

## Expected Artifacts

- v3 manifest, status, loop state, runbook, and contracts.
- active pointer in `loops/current_loop.json`.
- v3 entry in `loops/registry.json`.
