# V3 Candidate LLM Wiki

This directory is the v3 candidate output. It is not a repository-level promoted stable `llm_wiki`.

Drafts live under `drafts/`. Accepted candidate cards live under `kb/`.

Until v3 decides how to import prior candidates, the v2 output at `loops/v2_llm_wiki_loop_20260525/outputs/llm_wiki` is read-only comparison input.
