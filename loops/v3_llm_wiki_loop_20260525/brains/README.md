# V3 Brains

This directory holds role-local state for v3.

Brains are coordination surfaces, not independent source-of-truth products. The canonical loop state remains `loop_state.json`, `queues/`, and artifacts under `outputs/llm_wiki/`.

Roles:

- `production`
- `similarity`
- `audit`
- `ops`
