# Closed LLM Wiki Loop Capsule

- `archived_at`: `2026-05-25T19:54:38+08:00`
- `archive_reason`: user requested that the current loop products become an archived loop capsule so a new loop can start from a fresh capsule.
- `status`: closed historical loop products.

This capsule contains the previous active loop contents plus the candidate
`llm_wiki` output that used to live at the repository root.

## Included Product Classes

- root loop docs and state files;
- brain mailbox state, hook, tool, logs, and smoke evidence;
- decisions, iterations, queues, reports, plans, reflections, and audits;
- system prompts and task templates;
- local loop tools and user-insights fallback traces.
- candidate output in `outputs/llm_wiki/`.

## Use Policy

Do not treat this capsule as active loop state. A future loop may import a file
from this capsule only by writing down why it is being imported and what role it
plays in the new loop.
