# LLM Wiki Loop Workspace

`status`: `new_loop_not_started`

This directory is now the empty active workspace for the next loop.

The previous loop's active products were moved to:

`llm_wiki/loop/legacy/20260525-closed-llm-wiki-loop-products/`

## Recovery Rule

Do not recover the current loop from `legacy/` by default. Legacy is historical
evidence only.

A new loop should first define:

- loop objective and non-goals;
- active state schema;
- run / task / audit layout;
- where run-scoped audits should live;
- which old artifacts, if any, are intentionally imported from legacy.

Until that charter exists, this workspace should remain minimal.
